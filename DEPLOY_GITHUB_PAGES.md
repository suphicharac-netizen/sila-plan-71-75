# วิธีนำเว็บแอปขึ้น GitHub Pages

โปรเจกต์นี้ตั้งค่าให้ deploy ขึ้น GitHub Pages แบบอัตโนมัติผ่าน GitHub Actions
ไว้แล้ว (ไฟล์ `.github/workflows/deploy.yml`) ทำตามขั้นตอนนี้ครั้งเดียว
หลังจากนั้นทุกครั้งที่ push โค้ดขึ้น branch `main` เว็บจะอัปเดตให้เองอัตโนมัติ

## ขั้นตอนที่ 1: สร้าง repository บน GitHub

1. เข้า https://github.com/new
2. ตั้งชื่อ repo เช่น `sila-plan-app` (จำชื่อนี้ไว้ จะได้ URL เป็น
   `https://<ชื่อบัญชี>.github.io/sila-plan-app/`)
3. เลือก Public (GitHub Pages ฟรีใช้ได้กับ repo แบบ public) กด Create repository
   โดยยังไม่ต้องติ๊ก "Add a README"

## ขั้นตอนที่ 2: อัปโหลดโค้ดขึ้น repo

เปิด Terminal ในโฟลเดอร์โปรเจกต์นี้ แล้วรันทีละบรรทัด (แทนที่
`<ชื่อบัญชี>` และ `<ชื่อ repo>` ด้วยของจริง):

```bash
git init
git add .
git commit -m "Initial commit: ระบบแผนพัฒนาท้องถิ่น เทศบาลเมืองศิลา"
git branch -M main
git remote add origin https://github.com/<ชื่อบัญชี>/<ชื่อ repo>.git
git push -u origin main
```

## ขั้นตอนที่ 3: เปิดใช้งาน GitHub Pages แบบ Actions

1. ไปที่หน้า repo บน GitHub → แท็บ **Settings**
2. เมนูซ้าย เลือก **Pages**
3. หัวข้อ "Build and deployment" → ช่อง **Source** เลือก
   **"GitHub Actions"** (ไม่ใช่ "Deploy from a branch")
4. ไม่ต้องตั้งค่าอะไรเพิ่ม ระบบจะรัน workflow ที่มากับโปรเจกต์นี้ให้เอง

## ขั้นตอนที่ 4: รอ build แล้วเข้าเว็บ

1. ไปที่แท็บ **Actions** ของ repo จะเห็น workflow "Deploy to GitHub Pages"
   กำลังรันอยู่ (ใช้เวลาประมาณ 1-2 นาที)
2. เมื่อสถานะขึ้นเครื่องหมายถูกสีเขียว กลับไปที่ Settings → Pages จะเห็น
   ลิงก์เว็บไซต์ขึ้นด้านบน (รูปแบบ
   `https://<ชื่อบัญชี>.github.io/<ชื่อ repo>/`)
3. เปิดลิงก์นั้น จะเจอหน้า Login ของระบบ — ทดสอบด้วยบัญชีตัวอย่างในไฟล์
   README.md (รหัสผ่านทดสอบ: `sila1234`)

## การอัปเดตเว็บครั้งต่อไป

แก้โค้ดแล้ว push ขึ้น `main` ตามปกติ เว็บจะ build และ deploy ให้เองทุกครั้ง
ไม่ต้องรันคำสั่ง build เองบนเครื่อง:

```bash
git add .
git commit -m "อธิบายสิ่งที่แก้ไข"
git push
```

## หมายเหตุสำคัญก่อนใช้งานจริง

- ข้อมูลของระบบยังเก็บอยู่ใน **localStorage ของเบราว์เซอร์แต่ละเครื่อง**
  เท่านั้น การ deploy ขึ้น GitHub Pages ทำให้ "เข้าเว็บได้จากที่ไหนก็ได้"
  แต่ **ยังไม่ทำให้ข้อมูลซิงก์ข้ามเครื่อง/ข้ามผู้ใช้กัน** — แต่ละคนที่เปิด
  เว็บจะเห็นข้อมูลชุดของตัวเอง ควรอ่านหัวข้อ "ยังเหลือให้ทำต่อ" ด้านล่าง
- รหัสผ่านตัวอย่าง (`sila1234`) เป็นของ demo เท่านั้น ควรเปลี่ยนก่อนใช้งาน
  จริง โดยเข้าเมนู "จัดการผู้ใช้งาน" (ต้องเพิ่มฟีเจอร์เปลี่ยนรหัสผ่านต่อ
  เพราะเวอร์ชันนี้ยังตั้งรหัสผ่านผ่านโค้ดเท่านั้น)
- GitHub Pages เป็น static hosting ล้วนๆ ไม่มี backend/database ให้ ถ้า
  ต้องการให้หลายคนใช้งานพร้อมกันและเห็นข้อมูลชุดเดียวกันจริง ต้องทำ backend
  จริง (เช่น เชื่อมกับ Google Apps Script ตามที่แนะนำไว้ก่อนหน้านี้) แยก
  ต่างหากจากขั้นตอน deploy นี้
